/**
 * Used to broadcast to others that a user has joined the room
 */
export const USER_JOINED = "USER_JOINED";

/**
 * Used to display a "welcome user" message
 */
export const WELCOME_USER = "WELCOME_USER";

/**
 * Used to tell the client to display a sign in screen
 */
export const SIGN_IN = "SIGN_IN";
